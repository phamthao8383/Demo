CREATE TABLE [dbo].[TB_DISTRICT] (
    [ID]          INT           IDENTITY (1, 1) NOT NULL,
    [ID_PROVINCE] INT           NULL,
    [Name]        NVARCHAR (50) NULL,
    CONSTRAINT [PK_TB_DISTRICT] PRIMARY KEY CLUSTERED ([ID] ASC),
    CONSTRAINT [FK_TB_DISTRICT_TB_PROVINCE] FOREIGN KEY ([ID_PROVINCE]) REFERENCES [dbo].[TB_PROVINCE] ([ID])
);

