CREATE TABLE [dbo].[TB_COMMUNE] (
    [ID]          INT           IDENTITY (1, 1) NOT NULL,
    [ID_DISTRICT] INT           NULL,
    [Name]        NVARCHAR (50) NULL,
    CONSTRAINT [PK_TB_COMMUNE] PRIMARY KEY CLUSTERED ([ID] ASC),
    CONSTRAINT [FK_TB_COMMUNE_TB_DISTRICT] FOREIGN KEY ([ID_DISTRICT]) REFERENCES [dbo].[TB_DISTRICT] ([ID])
);

